#!/usr/bin/env ruby
# frozen_string_literal: true

remote = ARGV[0]

if remote.nil? || remote.empty?
  warn 'usage: git-worktree-init [remote]'
  exit 1
end

success = system('git', 'clone', '--bare', remote, '.bare')
exit 1 unless success

File.write('.git', "gitdir: ./.bare\n", mode: 'a')
